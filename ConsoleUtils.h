#ifndef CONSOLEUTILS_H
#define CONSOLEUTILS_H

class ConsoleUtils {
public:
    static int getTerminalWidth();  // Fetches the terminal width
    static int getTerminalHeight();  // Fetches the terminal height
};

#endif
